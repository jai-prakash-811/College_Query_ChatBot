# College Query Chatbot

- Keep the application offline-first and free of required API keys.
- Keep FAQ content in `data/faq.json` so staff can update answers without changing Python code.
- Run tests with `pytest` and launch the interface with `streamlit run app.py`.
